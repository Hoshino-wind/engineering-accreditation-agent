# 技术架构文档 v0.6

> 工程认证智能体 — 实验教学证据治理与持续改进平台
> 更新日期：2026-07-31 | 状态：MVP 阶段，代码已跑通

---

## 1. 系统概述

### 1.1 产品定位

面向高校工程教育认证场景，通过 AI 智能体 + 知识图谱，将学校上传的教学材料自动解析为结构化能力图谱，与 2024 认证标准对标，输出覆盖度诊断、改进建议和自评报告。

### 1.2 核心价值

- **标尺内置**：2024 认证标准（11 条毕业要求 + 22 个能力指标）预置为系统标尺
- **AI 提取**：上传材料 → LLM 自动提取课程/实验/知识点/资源节点
- **图谱诊断**：实时计算覆盖度、达成度，生成"为什么不符合"的可信解释
- **教师审核闸门**：AI 给出的永远是 pending 候选，由教师裁决

### 1.3 当前状态（如实标注）

| 能力 | 状态 | 说明 |
|------|------|------|
| 前端 8 模块 UI | ✅ 已完成 | React 19 + TS + Ant Design 5，FSD 架构 |
| 确定性算法 | ✅ 已实现 | 覆盖度分析、达成度计算、数据驱动解释生成 |
| 后端 API 脚手架 | ✅ 已完成 | FastAPI 分层架构，6 个 LLM API 端点 |
| LLM 接入 | ⚠️ 架构就绪 | llmClient.ts 已改为 fetch 后端 API，无 API Key 时降级 mock |
| RAG 向量检索 | ⚠️ 架构就绪 | InMemoryRAGRepository 内存实现，支持替换为 Qdrant |
| 数据 | ⚠️ 模拟 | 学校材料、图谱节点、识别候选均为硬编码原型数据 |
| 后端 LLM 调用 | ⚠️ 同步 | FastAPI 路由内 async 调用，后续改 Celery 异步队列 |

---

## 2. 系统模块

### 2.1 模块全景

```
┌─────────────────────────────────────────────────────────┐
│                      前端（React 19）                      │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐  │
│  │ M1   │ │ M3   │ │ M4   │ │ M2   │ │ M5   │ │ M6   │  │
│  │ 总览  │ │ 资源  │ │ 图谱  │ │ 识别  │ │ 诊断  │ │ 评价  │  │
│  └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘  │
│     │        │        │        │        │        │        │
│  ┌──┴────────┴────────┴────────┴────────┴────────┴───┐  │
│  │           shared/api/llmClient.ts                 │  │
│  │     fetch → 后端 API / 自动降级 → 本地 mock        │  │
│  └───────────────────────┬───────────────────────────┘  │
└──────────────────────────┼──────────────────────────────┘
                           │ HTTP
┌──────────────────────────┼──────────────────────────────┐
│                    后端（FastAPI）                         │
│  ┌───────────────────────┴───────────────────────────┐   │
│  │              /api/v1/llm/*  路由层                │   │
│  │  extract | infer-relations | explain | suggest    │   │
│  │  report  | rag/search                              │   │
│  └──┬──────────┬──────────┬──────────┬───────────────┘   │
│     │          │          │          │                    │
│  ┌──┴───┐ ┌───┴──┐ ┌────┴───┐ ┌────┴──────────────┐     │
│  │ LLM  │ │ RAG  │ │系统/资源│ │ 识别/诊断/改进      │     │
│  │Client│ │Search│ │  模块   │ │  模块               │     │
│  └──┬───┘ └──┬───┘ └────────┘ └────────────────────┘     │
│     │          │                                         │
│  ┌──┴───┐ ┌───┴──────────┐                              │
│  │Deep- │ │InMemoryRAG   │                              │
│  │Seek  │ │(→Qdrant)     │                              │
│  │API   │ │              │                              │
│  └──────┘ └──────────────┘                              │
└─────────────────────────────────────────────────────────┘
```

### 2.2 前端模块（FSD 架构）

| 模块 | 路径 | 功能 |
|------|------|------|
| **M1 总览** | `views/overview` | 认证状态指标卡、就绪度看板、近期活动 |
| **M3 教学资源** | `views/resources` | 材料上传、资源列表、筛选 |
| **M4 图谱视图** | `views/graph` | 能力图谱可视化、节点/边画布、审核面板 |
| **M2 识别审核** | `views/recognition` | AI 推荐关系候选队列、置信度圆环、证据审核 |
| **M5 图谱诊断** | `views/diagnostics` | 覆盖度分析、缺口解释、断裂路径 |
| **M6 达成度评价** | `views/evaluations` | 达成度卡片、计算追踪、评价配置 |
| **M7 教学改进** | `views/improvements` | AI 建议生成、改进案例追踪、闭环评估 |
| **M8 认证支撑** | `views/support` | 自评报告 AI 生成、完整性检查、章节预览 |

### 2.3 后端模块

| 模块 | 路径 | 功能 |
|------|------|------|
| **system** | `modules/system` | 系统状态、健康检查 |
| **resources** | `modules/resources` | 教学资源 CRUD |
| **recognition** | `modules/recognition` | 识别候选列表、审核裁决 |
| **diagnostics** | `modules/diagnostics` | 诊断发现列表、裁决 |
| **llm** | `modules/llm` | LLM 调用 + RAG 检索（6 个 API 端点） |

后端采用分层架构：
```
routes/        → API 路由（FastAPI Router）
application/   → 用例编排（Use Case + Port 接口）
domain/        → 领域模型（纯数据类，无框架依赖）
infra/         → 基础设施实现（内存存储 / LLM 客户端 / RAG 仓库）
contracts/     → 请求/响应 DTO（Pydantic 模型）
```

---

## 3. 技术选型

### 3.1 前端技术栈

| 层 | 技术 | 版本 | 选型理由 |
|---|------|------|---------|
| 框架 | React | 19 | 最新稳定版，并发渲染 |
| 语言 | TypeScript | 5.7 | 类型安全，ESLint 严格模式 |
| 构建 | Vite | 6.x | HMR 快，生态成熟 |
| UI 库 | Ant Design | 5.x | 企业级组件，表格/表单/抽屉覆盖全 |
| 图谱可视化 | 自研 Canvas + SVG | - | 节点拖拽、边渲染、布局算法 |
| 架构 | FSD（Feature-Sliced Design） | - | 层次隔离，features/entities/views 分层 |
| 状态 | React hooks（useState/useEffect） | - | Demo 阶段无需全局状态库 |

### 3.2 后端技术栈

| 层 | 技术 | 版本 | 选型理由 |
|---|------|------|---------|
| 框架 | FastAPI | 0.140+ | 异步、自动 OpenAPI 文档 |
| 语言 | Python | 3.12+ | 类型注解 + Pydantic |
| HTTP 客户端 | httpx | 0.27+ | 异步调用 LLM API |
| 数据验证 | Pydantic Settings | 2.x | 环境变量管理 |
| LLM | DeepSeek / OpenAI 兼容 | - | API 兼容格式，可切换供应商 |
| 向量库 | InMemoryRAG（→Qdrant） | - | Demo 内存实现，生产换 Qdrant |
| 异步队列 | Celery（规划） | - | LLM 长任务异步化（后续接入） |

### 3.3 LLM / RAG 选型

| 组件 | Demo 阶段 | 生产目标 |
|------|----------|---------|
| LLM | mock 降级 / DeepSeek-V2 | DeepSeek-V2 或 Qwen2.5-72B |
| Embedding | TF 向量（内存） | bge-m3 / text-embedding-3-small |
| 向量库 | InMemoryRAG（余弦相似度） | Qdrant（Docker 部署） |
| Prompt | 后端构建，JSON 输出 | 同 + few-shot + RAG 注入 |
| 降级策略 | 无 API Key → mock 数据 | API 失败 → 重试 → 降级模板 |

---

## 4. 核心接口

### 4.1 LLM API 端点

所有端点前缀：`/api/v1/llm`

| 端点 | 方法 | 功能 | 智能体 |
|------|------|------|--------|
| `/extract` | POST | 从材料文本提取结构化节点 | M4 材料解析 |
| `/infer-relations` | POST | 推断学校节点与标准指标的支撑关系 | M4→M2 关系推理 |
| `/explain` | POST | 基于缺口事实 + RAG 生成诊断叙述 | M5 图谱诊断 |
| `/suggest` | POST | 基于缺口数据生成改进建议 | M7 改进建议 |
| `/report` | POST | 基于全图谱数据生成自评报告章节 | M8 报告生成 |
| `/rag/search` | POST | RAG 向量检索相关文本块 | 所有智能体共用 |

### 4.2 统一响应结构

```json
{
  "data": [...],
  "model": "deepseek-chat",
  "usage": { "prompt_tokens": 1200, "completion_tokens": 400, "total_tokens": 1600 },
  "latency": 2300
}
```

### 4.3 前端 llmClient.ts 调用链

```
UI 页面 → features/*/model/*.ts → llmClient.ts → fetch /api/v1/llm/*
                                                      ├─ 成功 → 返回真实 LLM 响应
                                                      └─ 失败 → 降级为本地 mock
```

| 前端调用函数 | 后端端点 | 调用方 |
|-------------|---------|--------|
| `extractNodesViaLLM()` | `/llm/extract` | `extractNodes.ts` |
| `inferRelationsViaLLM()` | `/llm/infer-relations` | 图谱构建（待接入） |
| `generateSuggestionsViaLLM()` | `/llm/suggest` | `generateSuggestion.ts` |
| `generateReportViaLLM()` | `/llm/report` | `generateReport.ts` |

### 4.4 已有后端接口（非 LLM）

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/v1/system/status` | GET | 系统状态 |
| `/api/v1/resources` | GET | 教学资源列表 |
| `/api/v1/resources/{id}` | GET | 资源详情 |
| `/api/v1/recognition/candidates` | GET | 识别候选列表 |
| `/api/v1/recognition/candidates/{id}/review` | POST | 审核候选 |
| `/api/v1/diagnostics/findings` | GET | 诊断发现列表 |
| `/api/v1/diagnostics/findings/{id}/decide` | POST | 裁决诊断 |

---

## 5. 数据流转逻辑

### 5.1 端到端核心流程

```
① 内置标准标尺
  2024认证标准（11条毕业要求 + 22个能力指标）
  → standardLibrary.ts（前端硬编码）
  → RAG ingest_standard_library（后端向量化）

② 学校上传材料
  培养方案 / 课程大纲 / 实验指导书
  → M3 上传页（前端）
  → RAG ingest（后端分块向量化）

③ AI 提取节点（M4 材料解析智能体）
  材料文本 → POST /llm/extract
  → LLM 输出 ExtractionItem[]（code/name/kind/confidence/sourceExcerpt）
  → 教师预览 → 确认入图

④ 关系推理（M4→M2 关系推理智能体）
  学校节点 + 标准节点 → POST /llm/infer-relations
  → LLM 输出 RelationItem[]（source_id/target_id/strength/confidence/reasoning）
  → 生成 pending 状态的 SUPPORTS 边
  → 进入 M2 识别审核队列

⑤ 教师审核（M2 识别审核）
  候选队列 → 教师逐条裁决（approve/reject/modify）
  → approved 边入图，reviewStatus 变为 approved

⑥ 覆盖度分析（M5 确定性算法）
  图谱 approved 边 → analyzeCoverage()
  → 每个能力指标的支撑课程数、强度加权（strong=3/medium=2/weak=1）
  → 状态判定：covered（≥3）/ partial（1-2）/ gap（0）

⑦ 达成度计算（M6 确定性算法）
  图谱数据 → calculateAttainmentFromGraph()
  → 总支撑强度 / 9 归一化 → 达成度 0-1
  → 对比 0.7 阈值 → 达标/预警/不达标

⑧ 诊断解释（M5 规则化 + LLM）
  缺口数据 → generateExplanation()（纯 TS 规则拼装）
  → 实时算出：缺口原因、断裂路径、缺失证据类型
  → POST /llm/explain（+ RAG 上下文）→ LLM 生成连贯叙述

⑨ 改进建议（M7 LLM 生成）
  缺口列表 → POST /llm/suggest
  → LLM 输出 rootCause / suggestion / expectedEffect
  → 教师审阅 → 形成改进案例

⑩ 报告生成（M8 LLM 生成）
  全图谱数据 + 诊断结果 + 改进记录 → POST /llm/report
  → LLM 生成各毕业要求的自评报告章节叙述
  → 前端预览 → 导出
```

### 5.2 数据模型

#### 图谱节点（AbilityGraphNode）

```typescript
{
  id: string;           // 节点 ID（如 "co-ds"）
  kind: 'graduation_requirement' | 'competency' | 'course' | 'experiment' | 'knowledge' | 'resource';
  code: string;         // 编号（如 "GR-01"、"C-01-01"、"CO-DS"）
  name: string;
  description?: string;
  creditHours?: number; // 课程学分
  standardRef?: string;  // 标准引用
}
```

#### 图谱边（AbilityGraphEdge）

```typescript
{
  id: string;
  source: string;       // 起点节点 ID
  target: string;       // 终点节点 ID
  type: 'CONTAINS' | 'SUPPORTS' | 'COVERS_KNOWLEDGE' | 'USES_RESOURCE' | 'BELONGS_TO';
  strength?: 'strong' | 'medium' | 'weak';
  sourceType: 'rule' | 'ai' | 'manual';  // 建边方式
  reviewStatus: 'approved' | 'pending' | 'rejected';
  confidence?: number;  // AI 推荐置信度
  aiReasoning?: string; // AI 推理依据
}
```

#### LLM 响应（LLMResponse<T>）

```typescript
{
  data: T;
  model: string;        // 如 "deepseek-chat" 或 "deepseek-v2 (mock)"
  usage: { prompt_tokens: number; completion_tokens: number };
  latency: number;       // 毫秒
}
```

### 5.3 RAG 数据流

```
知识库内容：
├── 2024 认证标准原文（11 条毕业要求 + 22 个指标描述）
├── 学校上传材料（培养方案/大纲/实验书，分块向量化）
├── 能力图谱节点（课程/实验/知识点描述）
└── 历史改进案例（后续积累）

检索流程：
智能体调用 → query → RAG search(top_k=3) → 返回相关文本块
→ 文本块注入 LLM Prompt → 生成可溯源的回答
```

---

## 6. 智能体架构

### 6.1 五个智能体

| # | 智能体 | 输入 | 输出 | 实现状态 |
|---|--------|------|------|---------|
| 1 | 材料解析（M3→M4） | 材料文本 | ExtractionItem[] | ⚠️ 后端 API 就绪，无 Key 降级 mock |
| 2 | 关系推理（M4→M2） | 学校节点+标准节点 | RelationItem[] | ⚠️ 后端 API 就绪，前端待接入 |
| 3 | 图谱诊断（M5） | 缺口事实+RAG上下文 | ExplanationItem[] | ✅ 规则化解释已实现，LLM 增强就绪 |
| 4 | 改进建议（M7） | 缺口列表 | SuggestionItem[] | ⚠️ 后端 API 就绪，无 Key 降级 mock |
| 5 | 报告生成（M8） | 全图谱数据 | ReportChapterItem[] | ⚠️ 后端 API 就绪，无 Key 降级 mock |

### 6.2 确定性算法 vs AI 能力分工

| 能力 | 类型 | 说明 |
|------|------|------|
| 覆盖度分析 | 确定性算法 | `analyzeCoverage()` — strong=3/medium=2/weak=1 加权 |
| 达成度计算 | 确定性算法 | `calculateAttainmentFromGraph()` — 总强度/9 归一化 |
| 缺口解释 | 规则化 + AI | `generateExplanation()` 规则拼装 + LLM 生成连贯叙述 |
| 节点提取 | AI | LLM 从材料文本提取结构化节点 |
| 关系推理 | AI | LLM 推断支撑关系 + 置信度 + 推理依据 |
| 建议生成 | AI | LLM 基于缺口数据生成根因分析 + 改进方案 |
| 报告生成 | AI | LLM 基于全图谱数据生成自评报告叙述 |

**铁律**：确定性算法不加 AI。覆盖度、达成度、0.7 阈值对比是尺子量出来的硬数据，绝不能让模型自由发挥。

### 6.3 降级策略

```
LLM 调用 → 30s 超时 → 失败
  ↓
自动降级为 mock 数据（与 seed 数据对齐）
  ↓
前端显示 model: "deepseek-v2 (mock)" 标签
  ↓
Demo 流程继续，不中断
```

---

## 7. 前端架构

### 7.1 FSD 分层

```
app/           → 应用入口、路由、主题、全局样式
  ├── layouts/    → AppShell（侧栏+顶栏+内容区）
  ├── router/     → appRouter（8 个路由）
  ├── styles/     → global.css（设计 token 系统）
  └── theme.ts    → Ant Design 主题定制

entities/      → 领域实体（数据模型 + 原型数据 + UI 标签）
  ├── ability-graph/       → 图谱节点/边/标准库
  ├── attainment-evaluation/
  ├── diagnostic-finding/
  ├── recognition-candidate/
  └── ...

features/      → 业务功能（跨实体编排）
  ├── analyze-coverage/     → 覆盖度分析 + 解释生成
  ├── calculate-attainment/ → 达成度计算
  ├── extract-nodes/        → 节点提取
  ├── generate-improvement-suggestion/
  ├── generate-report/
  └── ...

views/         → 页面（组合 features + widgets）
  ├── overview/    → M1
  ├── resources/   → M3
  ├── graph/       → M4
  ├── recognition/ → M2
  ├── diagnostics/ → M5
  ├── evaluations/ → M6
  ├── improvements/→ M7
  └── support/     → M8

widgets/       → 复合 UI 组件
  ├── ability-graph-canvas/  → 图谱画布
  ├── overview-metrics/      → 指标卡
  └── ...

shared/        → 共享基础设施
  ├── api/         → client.ts（API 客户端）+ llmClient.ts（LLM 调用）
  └── config/      → env.ts（环境变量）
```

### 7.2 设计 Token 系统

`global.css` 定义了完整的 CSS 变量体系：

- **颜色**：品牌色 `--color-primary` / 中性色 10 档 / 语义色（success/warning/danger）
- **字号**：6 档（xs/sm/base/lg/xl/2xl）
- **间距**：4/8 栅格（4/8/12/16/24/32/48px）
- **圆角**：sm(4)/md(8)/lg(12)/xl(16)
- **阴影**：3 档（card/hover/drawer）
- **过渡**：统一 `--transition-base`

### 7.3 图谱可视化

自研 Canvas + SVG 实现：
- **布局算法**：`layout.ts` — 按节点类型分层布局
- **节点渲染**：`AbilityGraphNodeCard` — 按类型着色（毕业要求=蓝/课程=绿/实验=橙/知识点=紫）
- **边渲染**：SVG path，按 strength 着色（strong=粗实线/medium=中实线/weak=虚线）
- **交互**：节点拖拽、点击选中、右侧详情抽屉

---

## 8. 后端架构

### 8.1 分层结构（以 llm 模块为例）

```
modules/llm/
  ├── domain/
  │   └── models.py          → 领域模型（纯数据类）
  ├── application/
  │   ├── ports.py            → LLMClientPort 抽象接口
  │   └── rag_port.py         → RAGSearchPort 抽象接口
  ├── infra/
  │   ├── llm_client.py       → OpenAICompatibleLLMClient（真实 API + mock 降级）
  │   ├── rag_inmemory.py     → InMemoryRAGRepository（TF 向量 + 余弦相似度）
  │   └── mock_data.py        → 降级数据（与前端 seed 对齐）
  ├── contracts/
  │   └── llm.py              → Pydantic 请求/响应模型
  └── routes/
      └── llm.py              → FastAPI 路由（6 个端点）
```

### 8.2 依赖注入

```python
# main.py
llm_config = LLMConfig()                           # 从环境变量读取
llm_client = OpenAICompatibleLLMClient(config=llm_config)
rag_repo = InMemoryRAGRepository()

def provide_llm_client() -> LLMClientPort:
    return llm_client

def provide_rag_search() -> RAGSearchPort:
    return rag_repo

app.include_router(
    create_llm_router(provide_llm_client, provide_rag_search),
    prefix="/api/v1",
)
```

### 8.3 环境配置

```python
# config.py (EA_ 前缀环境变量)
EA_LLM_API_KEY=              # API 密钥（留空则降级 mock）
EA_LLM_API_BASE_URL=         # https://api.deepseek.com/v1
EA_LLM_MODEL=                # deepseek-chat
EA_LLM_EMBEDDING_API_KEY=    # Embedding 密钥
EA_LLM_EMBEDDING_BASE_URL=   # Embedding API 地址
EA_LLM_EMBEDDING_MODEL=      # text-embedding-3-small
EA_QDRANT_URL=               # Qdrant 地址（留空用内存）
EA_QDRANT_COLLECTION=        # 集合名
```

---

## 9. 演进路线

### 9.1 当前 → 生产

| 阶段 | LLM | RAG | 异步 | 数据 |
|------|-----|-----|------|------|
| **当前 Demo** | mock 降级 | 内存 TF | 同步 | 硬编码 |
| **接 API Key** | 真实 DeepSeek | 内存 TF | 同步 | 硬编码 |
| **生产 MVP** | 真实 LLM | Qdrant 向量 | Celery 队列 | PostgreSQL |
| **正式版** | 多模型切换 | Qdrant + 语义 | Celery + SSE | PG + Neo4j |

### 9.2 待完成项

1. **接入真实 LLM**：后端 `.env` 填 `EA_LLM_API_KEY`，5 个智能体自动切换
2. **Qdrant 替换内存 RAG**：实现 `QdrantRAGRepository`，接口不变
3. **Celery 异步队列**：LLM 调用改走 worker，前端轮询/SSE
4. **文件解析**：PDF/DOCX → 文本提取（pypdf / python-docx）
5. **PostgreSQL 持久化**：替换内存存储
6. **用户认证**：JWT + 角色权限

---

## 10. 目录结构总览

```
工程开发/
├── apps/
│   ├── web/                    # 前端
│   │   ├── src/
│   │   │   ├── app/            # 应用层
│   │   │   ├── entities/       # 领域实体（10 个）
│   │   │   ├── features/       # 业务功能（20+ 个）
│   │   │   ├── views/          # 页面（8 个模块）
│   │   │   ├── widgets/        # 复合组件（18 个）
│   │   │   └── shared/        # 共享（API 客户端 + 环境配置）
│   │   └── package.json
│   ├── api/                    # 后端 FastAPI
│   │   ├── app/
│   │   │   ├── core/           # 配置
│   │   │   └── modules/        # 业务模块（5 个）
│   │   │       ├── system/
│   │   │       ├── resources/
│   │   │       ├── recognition/
│   │   │       ├── diagnostics/
│   │   │       └── llm/        # LLM + RAG（6 个 API 端点）
│   │   └── pyproject.toml
│   └── worker/                 # Celery Worker（脚手架）
└── pnpm-lock.yaml
```
